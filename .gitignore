# Unanchored, so it also covers the examples under `crates/*/examples/`, each of
# which is a cargo workspace with a `target/` of its own.
target/
/lib

# Unanchored, so it also covers the per-package installs the root Bun workspace
# hoists into. `bun.lock` is committed — CI installs `--frozen-lockfile`.
node_modules/

# wasm-bindgen output of `cargo task wasm-peer`; the package type-checks
# without it and the mesh e2e suite rebuilds it.
packages/fofoca-wasm/wasm/
