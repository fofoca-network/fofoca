{
  "name": "fofoca",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/chat/*"
  ],
  "scripts": {
    "typecheck:node": "tsc --noEmit -p packages/fofoca-api && tsc --noEmit -p packages/fofoca-ffi && tsc --noEmit -p examples/chat/tui",
    "typecheck:web": "tsc --noEmit -p packages/fofoca-wasm && tsc --noEmit -p examples/chat/web",
    "typecheck": "bun run typecheck:node && bun run typecheck:web",
    "test": "bun test packages"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
