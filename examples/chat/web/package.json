{
  "name": "fofoca-chat-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The chat example's browser half: fofoca-wasm behind a small chat page",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "serve": "bun run serve.ts"
  },
  "dependencies": {
    "fofoca-wasm": "workspace:*"
  }
}
